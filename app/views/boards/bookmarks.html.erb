  <div class="container">
    <div class="row">
      <div class="col-lg-10 offset-lg-1">
        <%= render 'boards/search_form', url: bookmarks_boards_path %>
      </div>
    <% if @bookmarks.present? %>
      <%= render partial: 'board', collection: @bookmarks %>
    <% else %>
      <p><%= t('.no_result') %></p>
    <% end %>
    </div>
    <%= paginate @bookmarks, theme: 'twitter-bootstrap-4' %>
  </div>